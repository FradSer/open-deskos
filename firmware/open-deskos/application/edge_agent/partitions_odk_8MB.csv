# CERBERUS 8MB layout — derived from partitions_8MB.csv (FR-16).
# Same principle as the 16MB variant: the `emote` SPIFFS is trimmed (2800K ->
# 700K, no panel on a headless board) and the reclaimed space becomes a
# dedicated 2M `packages` FAT partition at /packages. The single OTA app slot,
# `system`, and the `storage` DATA root keep their base sizes.
# Name,    Type, SubType, Offset,   Size
nvs,       data, nvs,     0xA000,   0x6000
otadata,   data, ota,     0x10000,  0x2000
phy_init,  data, phy,     0x12000,  0x1000
ota_0,     app,  ota_0,         ,   3800K
emote,     data, spiffs,        ,   700K
system,    data, fat,           ,   900K
storage,   data, fat,           ,   564K
packages,  data, fat,           ,   2M
