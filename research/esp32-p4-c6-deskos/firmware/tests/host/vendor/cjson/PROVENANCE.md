# Vendored cJSON — provenance

| Field | Value |
|---|---|
| Project | cJSON |
| Version | `1.7.18` |
| Upstream repo | https://github.com/DaveGamble/cJSON |
| License | MIT |
| Acquired | 2026-07-11 |
| Source of these files | Copied verbatim from the ESP-IDF 5.5.1 toolchain's bundled cJSON at `$IDF_PATH/components/json/cJSON/` (`cJSON.c`, `cJSON.h`). This is the same source as the ESP-IDF built-in `json` component, so host manifest-parsing tests exercise the same JSON parser the target firmware links. |

## Files

- `cJSON.c`, `cJSON.h` — verbatim.
