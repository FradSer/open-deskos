scopes:
    - name: app
      description: macOS OpenDeskOS application, CLI, plugins, health agents, and tests in app/; does not cover embedded device firmware.
    - name: firmware
      description: ESP-IDF device software, board support, and display drivers in firmware/; does not cover the macOS application.
